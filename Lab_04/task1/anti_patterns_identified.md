# Идентификация анти-паттернов
1. **Загрязнение глобального пространства:** Использование `var` и глобальных переменных (например, `currentUser`). [cite_start]Это ведет к конфликтам имен[cite: 303, 331].
2. [cite_start]**Жесткая связь (Tight Coupling):** Функция `saveData` напрямую вызывает `updateUI`, что мешает тестированию[cite: 305, 332].
3. [cite_start]**Дублирование кода:** Повторение логики валидации в разных функциях (нарушение DRY)[cite: 307, 333].
4. [cite_start]**setTimeout со строкой:** Использование `setTimeout("alert('Hi')", 1000)`, что работает как медленный и опасный `eval()`[cite: 326, 334].
5. [cite_start]**Технический долг:** Использование магических чисел и отсутствие структуры[cite: 306].
